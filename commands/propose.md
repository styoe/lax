---
description: Propose new Tickets for un-tracked GitHub work
argument-hint: "[days]"
---

Read `${CLAUDE_PLUGIN_ROOT}/workflows/propose.md` and execute it verbatim. Wherever the workflow refers to "the subcommand argument", use the value of `$ARGUMENTS` (an optional lookback window in days; default 7 if unset). Do not paraphrase — follow the workflow exactly, including its per-item approval discipline.
