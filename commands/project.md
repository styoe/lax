---
description: Switch the active lax project (GitHub repo + Project pair)
---

Read `${CLAUDE_PLUGIN_ROOT}/workflows/project.md` and execute it verbatim. Do not paraphrase — follow its instructions exactly. Pass `$ARGUMENTS` as "the subcommand argument" wherever the workflow refers to it.
