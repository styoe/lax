---
description: Read-only standup write-up of recent Ticket + GitHub activity in the active lax project
argument-hint: "[day|week]"
---

Read `${CLAUDE_PLUGIN_ROOT}/workflows/standup.md` and execute it verbatim. Wherever the workflow refers to "the subcommand argument", use the value of `$ARGUMENTS` (an optional window — `day` or `week`; default `week` if unset). Do not paraphrase — follow the workflow exactly.
